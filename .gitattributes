# Handle line endings automatically for files detected as text 
# and leave all files detected as binary untouched.
* text=false

# Never modify line endings of following type of files
*.sh -crlf
*.css -crlf
*.html -crlf
*.java -crlf
*.js -crlf
*.json -crlf
*.properties -crlf
*.txt -crlf
*.xml -crlf
*.sh -lf
*.css -lf
*.html -lf
*.java -lf
*.js -lf
*.json -lf
*.properties -lf
*.txt -lf
*.xml -lf
git c
#
# The above will handle all files NOT found below
#
# These files are text and should be normalized (Not Convert crlf => lf)


# These files are binary and should be left untouched
# (binary is macro for -text -diff)
*.class         binary
*.jar           binary
*.gif           binary
*.jpg           binary
*.png           binary